version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    volumes:
      - ./server/uploads:/app/uploads

  client:
    build: ./client
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://server:5000
